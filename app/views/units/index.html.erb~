<h1>Units for <%= @runner.name %></h1> 
<table>
  <tr> 
   <th>Description</th> 
   <th>Date</th>
 </tr> 
<% for unit in @units %> 
 <tr>
  <td><%=h unit.description %></td> 
  <td><%=h unit.date %></td> 
  <td><%= link_to 'Show', runner_unit_path(@runner, unit) %></td>
  <td> <%= link_to 'Edit', edit_runner_unit_path(@runner, unit) %> </td> 
  <td> <%= link_to 'Destroy', runner_unit_path(@runner, unit), :confirm => 'Are you sure?', :method => :delete %> </td>
 </tr>
<% end %> 
</table>
 <br /> 
<%= link_to 'New unit', new_runner_unit_path(@post) %> <%= link_to 'Back to Runner', @runner %>


